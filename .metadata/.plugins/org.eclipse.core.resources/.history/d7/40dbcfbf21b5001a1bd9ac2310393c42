package com.corebanking.controller;

import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PutMapping;

import com.corebanking.model.Response;

@FeignClient(name="payment-service", url="http://localhost:8282")
public interface PaymentServiceProxy {

	@PutMapping("/user/{id}/payment/direct?amount={amount}&targetAccNo={target}&targetCurrency={currency}")
	Response doLoalPayments(@PathVariable("amount") double amount,@PathVariable("targert") long AccNum,@PathVariable("currency") String cur);
}
